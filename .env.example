PORT=3003
DB_PATH=./serverrat.db

# BattleMetrics API (free tier). Token optional — public endpoints work without it.
BATTLEMETRICS_TOKEN=
BM_BASE_URL=https://api.battlemetrics.com

# Cache TTL for server listings, in seconds
CACHE_TTL_SECONDS=300

# Steam Web API key
STEAM_API_KEY=
